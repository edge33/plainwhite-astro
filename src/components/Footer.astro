---
import plainwhiteConfig from '../plainwhite.config';
import DarkMode from './DarkMode/DarkMode.astro';

interface Props {
    condensedClass: string;
}

const { condensedClass } = Astro.props;

const {
    plainwhite: {
        navigation,
        social_links: {
            linkedIn,
            twitter,
            github,
            email,
            flickr,
            gitlab,
            facebook,
            instagram,
            pinterest,
            soundcloud,
            youtube,
            telegram,
            dribbble,
            stackoverflow,
        },
    },
} = plainwhiteConfig;
---

<>
    <ul class=`social about-footer ${condensedClass}`>
        {
            github && (
                <a href={`https://github.com/${github}`} target="_blank">
                    <li>
                        <i class="icon-github-circled" />
                    </li>
                </a>
            )
        }
        {
            gitlab && (
                <a href={`https://gitlab.com/${gitlab}`} target="_blank">
                    <li>
                        <i class="icon-gitlab" />
                    </li>
                </a>
            )
        }
        {
            linkedIn && (
                <a
                    href={`https://www.linkedin.com/${linkedIn}`}
                    target="_blank"
                >
                    <li>
                        <i class="icon-linkedin-squared" />
                    </li>
                </a>
            )
        }
        {
            twitter && (
                <a href={`https://twitter.com/${twitter}`} target="_blank">
                    <li>
                        <svg
                            xmlns="http://www.w3.org/2000/svg"
                            height="1em"
                            viewBox="0 0 512 512"
                        >
                            <path d="M389.2 48h70.6L305.6 224.2 487 464H345L233.7 318.6 106.5 464H35.8L200.7 275.5 26.8 48H172.4L272.9 180.9 389.2 48zM364.4 421.8h39.1L151.1 88h-42L364.4 421.8z" />
                        </svg>
                    </li>
                </a>
            )
        }

        {
            instagram && (
                <a href={`https://instagram.com/${instagram}`} target="_blank">
                    <li>
                        <i class="icon-instagram" />
                    </li>
                </a>
            )
        }
        {
            facebook && (
                <a href={`https://facebook.com/${facebook}`} target="_blank">
                    <li>
                        <i class="icon-facebook-squared" />
                    </li>
                </a>
            )
        }
        {
            youtube && (
                <a href={`https://youtube.com/${youtube}`} target="_blank">
                    <li>
                        <i class="icon-youtube" />
                    </li>
                </a>
            )
        }
        {
            pinterest && (
                <a href={`https://pinterest.com/${pinterest}`} target="_blank">
                    <li>
                        <i class="icon-pinterest-squared" />
                    </li>
                </a>
            )
        }
        {
            dribbble && (
                <a href={`https://dribbble.com/${dribbble}`} target="_blank">
                    <li>
                        <i class="icon-dribbble" />
                    </li>
                </a>
            )
        }
        {
            flickr && (
                <a href={`https://flickr.com/${flickr}`} target="_blank">
                    <li>
                        <i class="icon-flickr" />
                    </li>
                </a>
            )
        }
        {
            telegram && (
                <a href={`https://t.me/${telegram}`} target="_blank">
                    <li>
                        <i class="icon-telegram" />
                    </li>
                </a>
            )
        }
        {
            soundcloud && (
                <a
                    href={`https://soundcloud.com/${soundcloud}`}
                    target="_blank"
                >
                    <li>
                        <i class="icon-soundcloud" />
                    </li>
                </a>
            )
        }
        {
            email && (
                <a href={`mailto:${email}`} target="_blank">
                    <li>
                        <i class="icon-mail-alt" />
                    </li>
                </a>
            )
        }
        {
            stackoverflow && (
                <a
                    href={`https://stackoverflow.com/users/${stackoverflow}`}
                    target="_blank"
                >
                    <li>
                        <i class="icon-stack-overflow" />
                    </li>
                </a>
            )
        }
    </ul>
    {
        navigation && (
            <nav class={`navigation about-footer ${condensedClass}`}>
                <ul>
                    {navigation.map(({ url, title }) => (
                        <li>
                            <a href={url}>{title}</a>
                        </li>
                    ))}
                </ul>
            </nav>
        )
    }
    <p class={`about-footer ${condensedClass}`}>
        &copy;
        {new Date().getFullYear()}
    </p>
    <DarkMode condensedClass={condensedClass} />
</>
